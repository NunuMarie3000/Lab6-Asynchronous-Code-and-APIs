import React, { Component } from 'react'

import { Card } from 'react-bootstrap'

export default class Movies extends Component {
	constructor(props) {
		super(props)

		this.state = {
			show: false,
		}
	}
	//when they click the map button, i wanna show movies as different scrollable cards the user can view to the right side of the map
	render() {
		return (
			<>

			</>
		)
	}
}